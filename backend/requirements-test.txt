# Dependências para rodar a suíte de testes (não necessárias em produção)
-r requirements.txt

pytest>=8.0
pytest-cov>=5.0
httpx>=0.27          # exigido pelo fastapi.testclient.TestClient
mutmut>=3.0           # teste de mutação (Aula 9)
